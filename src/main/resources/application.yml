server:
  servlet:
    context-path: "/api"

spring:
  profiles:
    active: dev
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect